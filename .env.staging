# =============================================================================
# STAGING ENVIRONMENT CONFIGURATION (Dynamic Mapping)
# =============================================================================
# This file maps Amplify environment variables to NEXT_PUBLIC_ client variables
# Amplify Console provides the source values, this file creates client access
# Branch: staging.d200k2wsaf8th3.amplifyapp.com

# =============================================================================
# CORE ENVIRONMENT MAPPING (Amplify → Client)
# =============================================================================
# These variables reference Amplify Console environment variables
# Single source of truth: AWS Amplify Console for staging branch

# Environment identification
NEXT_PUBLIC_ENVIRONMENT=${NODE_ENV}

# Backend Configuration (maps from Amplify env vars)
NEXT_PUBLIC_BACKEND_SUFFIX=${BACKEND_SUFFIX}
NEXT_PUBLIC_GRAPHQL_URL=${GRAPHQL_URL}
NEXT_PUBLIC_USER_POOL_ID=${USER_POOL_ID}
NEXT_PUBLIC_USER_POOL_CLIENT_ID=${USER_POOL_CLIENT_ID}
NEXT_PUBLIC_S3_PUBLIC_BASE_URL=https://${S3_BUCKET}.s3.us-west-1.amazonaws.com

# Analytics Configuration (maps from Amplify env vars)
NEXT_PUBLIC_GA_MEASUREMENT_ID=${NEXT_PUBLIC_GA_MEASUREMENT_ID}
NEXT_PUBLIC_GTM_ID=${NEXT_PUBLIC_GTM_ID}
NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL}

# =============================================================================
# STAGING-SPECIFIC CONFIGURATION
# =============================================================================
# These values are specific to staging environment behavior

# Logging Configuration - Staging (balanced verbosity)
NEXT_PUBLIC_LOG_LEVEL=WARN
LOG_LEVEL=WARN

# Notification Configuration - Staging (debug mode for testing)
DEBUG_NOTIFICATIONS=true
DEBUG_EMAIL=staging-test@realtechee.com
DEBUG_PHONE=+17135919400
NOTIFICATION_RETRY_COUNT=2
NOTIFICATION_TIMEOUT=10000

# SendGrid Configuration - Staging (debug mode, non-production keys)
SENDGRID_API_KEY=SG.staging_key_here
FROM_EMAIL=staging-notifications@realtechee.com
REPLY_TO_EMAIL=dev-support@realtechee.com

# Twilio Configuration - Staging (debug mode, non-production keys)
TWILIO_ACCOUNT_SID=AC_staging_sid_here
TWILIO_AUTH_TOKEN=staging_token_here
TWILIO_FROM_PHONE=+1234567890

# Staging Features (production-like but with debug capabilities)
NEXT_PUBLIC_ENABLE_DEVTOOLS=true
NEXT_PUBLIC_SHOW_DEBUG_INFO=true
NEXT_PUBLIC_MOCK_EXTERNAL_APIS=false

# Performance Monitoring - Staging
NEXT_PUBLIC_PERFORMANCE_MONITORING=true
NEXT_PUBLIC_BUNDLE_ANALYZER=false
NEXT_PUBLIC_ERROR_REPORTING=true

# Security - Staging (production-like but less strict)
NEXT_PUBLIC_STRICT_MODE=true
NEXT_PUBLIC_CSRF_PROTECTION=warn
NEXT_PUBLIC_RATE_LIMITING=false

# Data Protection - Staging (allow test data, shared with dev)
ALLOW_TEST_DATA=true
AUTO_BACKUP_ENABLED=true
BACKUP_RETENTION_DAYS=14

# Compliance - Staging (relaxed for testing)
GDPR_COMPLIANCE=true
AUDIT_LOGGING=false
DATA_RESIDENCY=us-west-1

# Performance - Staging
CACHE_TTL=60
CDN_ENABLED=true
COMPRESSION_ENABLED=true

# Monitoring - Staging (more lenient thresholds)
HEALTH_CHECK_INTERVAL=120
ERROR_THRESHOLD=10
RESPONSE_TIME_THRESHOLD=15000

# =============================================================================
# STAGING ENVIRONMENT NOTES
# =============================================================================
# ARCHITECTURE:
# - Single source of truth: AWS Amplify Console environment variables
# - This file maps server-side vars to NEXT_PUBLIC_ client-side access
# - Dynamic mapping prevents drift between server and client configurations
# 
# AMPLIFY CONSOLE VARIABLES REQUIRED (staging branch):
# - NODE_ENV=staging
# - BACKEND_SUFFIX=irgzgwsfnzba3sfqtum5k2eyp4m
# - GRAPHQL_URL=https://xcl73f4ymzdbr06xcw72jnzr3a.appsync-api.us-west-1.amazonaws.com/graphql
# - USER_POOL_ID=us-west-1_NeGfFuVD7
# - USER_POOL_CLIENT_ID=13buiopad6u8rflp5fhi5vcc4
# - S3_BUCKET=amplify-d200k2wsaf8th3-st-realtecheeuseruploadsbuc-lollpnfn8hd5
# - AWS_REGION=us-west-1
#
# STAGING CHARACTERISTICS:
# - Debug notifications enabled for testing notification flows
# - Test data allowed and marked appropriately
# - Less strict security for easier testing and debugging
# - Production-like build but with development-friendly features
# - 14-day backup retention (shorter than production)
# - Error reporting enabled but audit logging disabled
# - URL: https://staging.d200k2wsaf8th3.amplifyapp.com
# =============================================================================