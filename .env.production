# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION (Dynamic Mapping)
# =============================================================================
# This file maps Amplify environment variables to NEXT_PUBLIC_ client variables
# Amplify Console provides the source values, this file creates client access
# Branch: production.d200k2wsaf8th3.amplifyapp.com

# =============================================================================
# CORE ENVIRONMENT MAPPING (Amplify → Client)
# =============================================================================
# These variables reference Amplify Console environment variables
# Single source of truth: AWS Amplify Console for production branch

# Environment identification
NEXT_PUBLIC_ENVIRONMENT=${NODE_ENV}

# Backend Configuration (maps from Amplify env vars)
NEXT_PUBLIC_BACKEND_SUFFIX=${BACKEND_SUFFIX}
NEXT_PUBLIC_GRAPHQL_URL=${GRAPHQL_URL}
NEXT_PUBLIC_USER_POOL_ID=${USER_POOL_ID}
NEXT_PUBLIC_USER_POOL_CLIENT_ID=${USER_POOL_CLIENT_ID}
NEXT_PUBLIC_S3_PUBLIC_BASE_URL=https://${S3_BUCKET}.s3.us-west-1.amazonaws.com

# Analytics Configuration (maps from Amplify env vars)
NEXT_PUBLIC_GA_MEASUREMENT_ID=${GA_MEASUREMENT_ID}
NEXT_PUBLIC_GTM_ID=${GA_MEASUREMENT_ID}
NEXT_PUBLIC_SITE_URL=${SITE_URL}

# =============================================================================
# PRODUCTION-SPECIFIC CONFIGURATION
# =============================================================================
# These values are specific to production environment behavior

# Logging Configuration - Production (Essential Only)
NEXT_PUBLIC_LOG_LEVEL=INFO
LOG_LEVEL=INFO

# Notification Configuration - Production (Live Mode)
DEBUG_NOTIFICATIONS=false
NOTIFICATION_RETRY_COUNT=3
NOTIFICATION_TIMEOUT=30000

# SendGrid Configuration - Production
SENDGRID_API_KEY=SG.production_key_here
FROM_EMAIL=notifications@realtechee.com
REPLY_TO_EMAIL=support@realtechee.com

# Twilio Configuration - Production
TWILIO_ACCOUNT_SID=AC_production_sid_here
TWILIO_AUTH_TOKEN=production_token_here
TWILIO_FROM_PHONE=+1234567890

# Production Features
NEXT_PUBLIC_ENABLE_DEVTOOLS=false
NEXT_PUBLIC_SHOW_DEBUG_INFO=false
NEXT_PUBLIC_MOCK_EXTERNAL_APIS=false

# Performance Monitoring - Production
NEXT_PUBLIC_PERFORMANCE_MONITORING=true
NEXT_PUBLIC_BUNDLE_ANALYZER=false
NEXT_PUBLIC_ERROR_REPORTING=true

# Security - Production (Strict)
NEXT_PUBLIC_STRICT_MODE=true
NEXT_PUBLIC_CSRF_PROTECTION=enforce
NEXT_PUBLIC_RATE_LIMITING=true

# Data Protection - Production
ALLOW_TEST_DATA=false
AUTO_BACKUP_ENABLED=true
BACKUP_RETENTION_DAYS=90

# Compliance - Production
GDPR_COMPLIANCE=true
AUDIT_LOGGING=true
DATA_RESIDENCY=us-west-1

# Performance - Production
CACHE_TTL=300
CDN_ENABLED=true
COMPRESSION_ENABLED=true

# Monitoring - Production
HEALTH_CHECK_INTERVAL=60
ERROR_THRESHOLD=5
RESPONSE_TIME_THRESHOLD=10000

# =============================================================================
# PRODUCTION ENVIRONMENT NOTES
# =============================================================================
# ARCHITECTURE:
# - Single source of truth: AWS Amplify Console environment variables
# - This file maps server-side vars to NEXT_PUBLIC_ client-side access
# - Dynamic mapping prevents drift between server and client configurations
# 
# AMPLIFY CONSOLE VARIABLES REQUIRED (production branch):
# - NODE_ENV=production
# - BACKEND_SUFFIX=yk6ecaswg5aehj3ev76xzpbfe
# - GRAPHQL_URL=https://lwccoiztzrervozzmsgavaql5i.appsync-api.us-west-1.amazonaws.com/graphql
# - USER_POOL_ID=us-west-1_UKszK3GQb
# - USER_POOL_CLIENT_ID=792b3vvu4or3pk0oemerbium36
# - S3_BUCKET=amplify-d200k2wsaf8th3-pr-realtecheeuseruploadsbuc-u5mq35rhcrmj
# - NEXT_PUBLIC_GA_MEASUREMENT_ID=G-P9K530YSRT
# - NEXT_PUBLIC_GTM_ID=(optional)
# - NEXT_PUBLIC_SITE_URL=https://www.realtechee.com
#
# PRODUCTION SECURITY REQUIREMENTS:
# - All API keys and tokens must be set to production values
# - Debug notifications are DISABLED - emails go to real recipients
# - Test data is FORBIDDEN in production tables
# - Strict security and CSRF protection enabled
# - All user interactions are logged for audit compliance
# - 90-day backup retention for compliance requirements
# - Rate limiting enabled to prevent abuse
# - Error reporting enabled for monitoring
# - URL: https://production.d200k2wsaf8th3.amplifyapp.com
# =============================================================================