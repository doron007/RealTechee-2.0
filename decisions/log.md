# Decision Log

Newest at top, append-only.

---

[2026-05-25] DECISION: Full AWS cross-region cost-scan and orphan cleanup. Confirmed account `403266990862` is in excellent post-sunset shape — no EC2/RDS/EBS/CloudFront/ELB/NAT/EIP/API-Gateway/WAF/Route53/Secrets/KMS-customer-keys in any region. Deleted: 2 orphan CFN stacks (`amplify-realtecheen8n-doron-sandbox-8bdb6ede52` from Oct 2025 n8n test + `amplify-realtecheeclone-production-sandbox-70796fa803` whose bucket was already gone), 106 orphan CloudWatch log groups (~600 MB) from deleted Amplify apps with retention=None, 1 orphan Cognito User Pool (`us-west-1_UKszK3GQb`, old prod with 2 users) + 1 orphan Identity Pool, emptied CDK assets bucket (286 MB current + 272 MB versions + 1708 delete-markers). Set retention=14d on the 9 surviving log groups. Preserved by user choice: the d200k2wsaf8th3-production-branch DELETE_FAILED stack that anchors the 2GB customer-images bucket (2,762 DDB references confirm it IS the live image server for Projects/Quotes/Requests/eSignatureDocuments). CORRECTED memory file: bucket `DeletionPolicy=Delete` not Retain — survived only because the auto-delete Lambda crashed first. Documented safe-cleanup-dance recipe in new memory `cfn-stuck-autodelete-custom-resource.md`. | REASONING: Free-tier expiration concern; goal was to minimize ongoing AWS spend going forward. Findings: actual cost is ~$0.91/mo (DDB $0.47 + KMS $0.39 + S3 $0.05), most of which is NOT free-tier-related and won't change at expiration. Cleanup saves ~$0.30/mo on CloudWatch storage. Kept d200k2wsaf8th3 bucket because migration to home server costs ~1-2 hours of work to save $0.60/year — not worth it given sunset mode. | CONTEXT: `/tmp/aws-scan/` (backups: bucket-policy/tags, full enumeration files), memory `[[project-sunset-state]]` (updated with actual footprint), `[[aws-amplify-delete-app-cascades-backend]]` (corrected — nothing survives by Retain), `[[cfn-stuck-autodelete-custom-resource]]` (NEW — safe-cleanup recipe), `[[runtime-endpoints]]` (corrected DeletionPolicy claim).

---

[2026-05-25] DECISION: Migrated RealTechee 2.0 frontend (www.realtechee.com + ai.realtechee.com) from AWS Amplify hosting to home server (`hetzim.ddns.net`, Docker + NPM proxy). Kept Amplify Gen2 backend on AWS (Cognito + AppSync + DynamoDB + S3 — all Always-Free at zero traffic). Stripped 5 Lambda functions from `amplify/backend.ts` and disabled the `postConfirmation` auth trigger because the CDK ESM assembler tripped on their `resource.ts` exports. Patched `utils/amplifyAPI.ts` to replace a hybrid Gen1/Gen2 `Amplify.configure` shape with the raw `amplify_outputs.json` (the hybrid form broke the Gen2 typed-client model registry — admin CRUD writes silently failed). Mid-migration `aws amplify delete-app` cascade-deleted the production backend (43 DDB tables + AppSync + Lambdas); recovered 2,672 of 2,673 items from local `./backups/migrations/*.json` (Aug 2025 snapshot) via `ampx sandbox --identifier restore` + boto3 `batch_write_item` import. Customer images (2.06 GB) survived in S3 due to DeletionPolicy:Retain. | REASONING: AWS 12-month free-tier expiring; business is closed so no active users; home server already hosts n8n/ollama/openwebui/supabase. Full Supabase migration would have required rewriting Amplify GraphQL client → Supabase client, which Doron explicitly excluded. Backend-on-AWS + frontend-on-home preserves the codebase as-is and lands at $0–$0.05/mo with zero code rewrites beyond the two unblocking edits (backend.ts strip + amplifyAPI.ts config fix). | CONTEXT: `.planning/STATE-FINAL.md`, `.planning/RESTORE-SUMMARY.md`, `.planning/MIGRATION-BRIEF.md`, memory `[[project-sunset-state]]`, `[[aws-amplify-delete-app-cascades-backend]]`, `[[restore-after-amplify-delete-app]]`, `[[runtime-endpoints]]`.
